"""
Data structure for representing secret shares of elements of finite sets
and univariate functions thereof, designed for use within secure multi-party
computation (MPC) protocol implementations.
"""
from __future__ import annotations
import doctest

class share(bytes):
    """
    Data structure for representing a secret share of an element from
    a finite set.
    """

if __name__ == '__main__':
    doctest.testmod() # pragma: no cover
